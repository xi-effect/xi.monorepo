import { LayoutPages } from 'kit/LayoutPages';
import { Password } from 'pkg.inputs.password';

const TestComponent = () => (
  <LayoutPages>
    <Password />
  </LayoutPages>
);

export default TestComponent;
